const pessoa = {
nome: "José",
idade: 9,
altura: 1.39
};

if (pessoa.idade >= 12 && pessoa.altura >= 1.40) { 
console.log(`${pessoa.nome} pode brincar.`)
} 
else {
console.log(`${pessoa.nome} não pode brincar neste brinquedo.`);
}
